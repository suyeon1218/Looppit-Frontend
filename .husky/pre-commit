#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 린트 체크 실행 중..."
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ 린트 실패: 커밋이 취소되었습니다."
  exit 1
fi

echo "✅ 린트 체크 통과"
