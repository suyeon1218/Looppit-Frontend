#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧹 이전 빌드 캐시 삭제 중..."
rm -rf .next

echo "🔨 빌드 실행 중..."
npm run build

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ 빌드 실패로 인해 push가 중단되었습니다."
  echo ""
  echo "다음 사항을 확인해주세요:"
  echo "  • 빌드 오류 메시지를 확인하여 문제를 해결해주세요"
  echo "  • TypeScript 타입 오류가 있는지 확인해주세요"
  echo "  • 모든 의존성이 올바르게 설치되었는지 확인해주세요 (npm install)"
  echo "  • 로컬에서 'npm run build'를 실행하여 오류를 확인해주세요"
  echo ""
  echo "문제를 해결한 후 다시 push를 시도해주세요."
  echo ""
  exit 1
fi

echo "✅ 빌드 성공"
